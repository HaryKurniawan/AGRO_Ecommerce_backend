import { Controller, Get, Query } from '@nestjs/common'; import { WhatsappService } from './infrastructure/whatsapp/whatsapp.service'; @Controller('test-wa') export class TestController { constructor(private wa: WhatsappService) {} @Get() async test(@Query('phone') phone: string, @Query('msg') msg: string) { const res = await this.wa.sendMessage(phone || '082117855189', msg || 'Ini pesan test dari backend!'); return { success: res, phone: phone || '082117855189' }; } }
