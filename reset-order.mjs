import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const order = await prisma.pesananEcom.findFirst({ orderBy: { createdAt: 'desc' }, include: { pengiriman: true } }); if (order) { await prisma.pengirimanPesananEcom.update({ where: { pesananId: order.id }, data: { status: 'IN_TRANSIT' } }); await prisma.pesananEcom.update({ where: { id: order.id }, data: { status: 'DIKIRIM' } }); console.log('Reset pesanan ' + order.id + ' ke IN_TRANSIT'); } } main();
